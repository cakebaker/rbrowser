[package]
name = "rbrowser"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
openssl = "0.10"
flate2 = "1.0" # gzip compression/decompression library
encoding = "0.2"
dirs = "3.0.2" # standard locations of directories for config, cache, and other data
